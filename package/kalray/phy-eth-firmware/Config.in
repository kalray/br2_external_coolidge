config BR2_PACKAGE_PHY_ETH_FIRMWARE
	bool "phy-eth-firmware"
	depends on BR2_kvx
	depends on BR2_LINUX_KERNEL
	help
		Coolidge Linux phy eth firmware

config BR2_PHY_ETH_FIRMWARE_COMPILE_FROM_SOURCE
	bool "Compile Phy/Eth firmware from source"
	depends on BR2_PACKAGE_PHY_ETH_FIRMWARE
	help
	  This option allows to compile the Phy/Eth firmware from source.
