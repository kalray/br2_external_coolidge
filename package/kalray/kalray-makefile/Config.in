config BR2_PACKAGE_KALRAY_MAKEFILE
	bool "kalray-makefile"
	depends on BR2_kvx
	help
	  The Kalray Makefile needed by various other Kalray packages.

config BR2_KALRAY_MAKEFILE_CUSTOM_TARBALL
	bool "Custom kalray-makefile tarball"
	depends on BR2_PACKAGE_KALRAY_MAKEFILE
	help
	  This option allows to specify a URL pointing to a kalray-makefile
	  source tarball. This URL can use any protocol recognized by
	  Buildroot, like http://, ftp://, file:// or scp://.

	  When pointing to a local tarball using file://, you may want
	  to use a make variable like $(TOPDIR) to reference the root of
	  the Buildroot tree.

config BR2_KALRAY_MAKEFILE_CUSTOM_TARBALL_LOCATION
	string "URL of custom kalray-makefile tarball"
	depends on BR2_KALRAY_MAKEFILE_CUSTOM_TARBALL
